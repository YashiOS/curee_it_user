<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- REMOVE these to disable Android 12 circle -->
        <!-- USE windowBackground instead to show your full splash image -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!--<item name="android:windowSplashScreenAnimatedIcon">@drawable/android12splash</item>-->
        <!--<item name="android:windowSplashScreenIconBackgroundColor">#1A1A1A</item>-->
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">#000000</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/android12splash</item>
        <item name="android:windowSplashScreenIconBackgroundColor">#000000</item>
    </style>
    <!-- Theme applied after app launches -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
